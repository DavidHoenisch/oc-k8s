{{- if .Values.dashboard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dashboard.name }}
spec:
  selector:
    app: {{ .Values.dashboard.n}}
  ports:
    - port: 8080
      targetPort: 8080
{{- end }}
